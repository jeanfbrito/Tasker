<div style="padding-top: 8px">
  <ul class="tasks zebra-list">

    <% @project.documents.each do |doc| %>
    <li>
      <input type="checkbox"> 
      <span class="title"><%= link_to doc.name, doc %> 
        <%= link_to icon(:pencil), "#editProject", :data => { :url => edit_project_path(@project), :toggle => "modal" }, :class => "edit-project-button" %>  
        <%= link_to icon(:remove), doc, :confirm => 'Are you sure?', :method => :delete %>
     </span> 
     <span class="meta">Created <em><%=time_ago_in_words(doc.created_at)%></em> ago by <em><%=doc.project.user.name%></em></span>
   </li>
   <% end %>
 </ul>
</div>